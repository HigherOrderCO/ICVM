def T = λt λf t
def F = λt λf f
def And = λpλq dup #a p1 p2 = p; (p1 q p2)
def Z = λs λz (z)
def S = λn λs λz (s n)
def Node = λa λb λn λl (n a b)
def Leaf = λn λl l

(Node (S (S (S Z))) Leaf)
// (Node Z Z)
// (S (S (S Z)))
// (S Z)
// (And T T)
